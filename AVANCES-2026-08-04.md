## Sesión 2026-08-04 — Fase 2 y 2b: componente ReadMore ("leer más")

### Lo que se hizo
Dos PR mergeados a main:
- **PR #21 `fase2-leer-mas`** (commit f3df5a2): componente `ReadMore.astro`
  en modo clamp, aplicado a la descripción de Producción Destacada del Home.
- **PR #22 `fase2b-leer-mas-parrafos`** (commit 35df026): modo párrafo,
  aplicado al subtítulo del hero y a las 3 descripciones de pilares.

Baseline regenerado desde `35df026`.

### El componente: dos modos, dos criterios de corte

**`mode="clamp"`** (default) — trunca a N líneas con `line-clamp`. El botón
aparece sólo si el texto realmente se desborda, lo decide el JS midiendo
`scrollHeight` vs `clientHeight` después de `document.fonts.ready`. Se usa
en Producción Destacada, donde la descripción es un bloque corrido.

**`mode="paragraph"`** — muestra el primer párrafo y esconde el resto. El
corte se decide en el build (¿hay más de un párrafo?), no en el navegador:
no mide nada, no espera fuentes, no hay reflow. Parser tolerante a `\n` y
`\n\n` como separador. Con un solo párrafo no se renderiza botón.

Por qué el modo párrafo para hero y pilares: una línea renderizada no es una
unidad de contenido. La medición lo mostró — el pilar Consciencia ocupa 42
líneas a 1440px y 133 a 768px. Un clamp de 4 líneas cae en un lugar distinto
en cada pantalla, casi siempre a mitad de frase. El párrafo cae siempre en
el mismo lugar, elegido por quien escribe.

### Decisión de contenido acordada con Caro
El primer párrafo de cada texto (hero + 3 pilares) es lo único que se lee sin
clickear. **Debe sostenerse solo: ser congruente y tener sentido sin
expandir.** Caro va a reescribir esos textos con ese criterio, cuando Santos
le dé el OK para usar el CMS.

### Medición previa — los números
Tomada antes de implementar, para elegir el modo de corte con datos.

| Bloque | ES chars | 1440px | 768px | 375px |
|---|---|---|---|---|
| hero_subtitle | 1998 (306 palabras) | 32 líneas | 62 | 48 |
| pillar_arte_desc | 987 | 25 | 78 | 33 |
| pillar_ciencia_desc | 1079 | 26 | 85 | 35 |
| pillar_consciencia_desc | 1713 | 42 | 133 | 55 |

### Bug de layout detectado — PARA FASE 3, no resuelto
Los pilares tienen **más líneas a 768px que a 375px**. Causa: la grilla
`md:grid-cols-3` pasa a 3 columnas exactamente en 768px, pero el contenedor
sigue angosto, así que cada columna de texto queda en **~123px de ancho** —
más angosta que la columna única de un celular de 375px. A ese ancho,
Consciencia ocupa 133 líneas.

Es preexistente, independiente de ReadMore, y hoy queda tapado por el corte
por párrafo. Fase 3 toca el Home: resolverlo ahí.

### `items-start` en la grilla de pilares
Sin él, el grid estira las 3 cards a la altura de la más alta. Antes de esta
sesión, la card de Arte tenía **442px de aire muerto**. Con el corte por
párrafo las 3 cards pasaron de 1333px a **378px** en reposo, y con
`items-start` expandir una no estira las otras dos.

Nota para cuando Caro reescriba: hoy las 3 quedan parejas en reposo por
coincidencia (los primeros párrafos rinden igual). Con textos nuevos, cada
card va a tomar su altura natural y pueden quedar desparejas. Es reversible
borrando la clase.

### Lecciones de proceso — las tres del día

**1. Un mensaje de commit en un pedido no es un `git add`.**
Dos rondas completas de trabajo (Fase 2 + sus correcciones), con
verificación y screenshots, quedaron viviendo **sólo en el working tree**.
Los pedidos terminaban en un bloque "COMMIT" que traía sólo el texto del
mensaje, sin comandos ejecutables. Se descubrió recién al pedir `git status`
antes del push: cero commits sobre origin/main.
El riesgo real: un `git checkout .` sin path se llevaba todo. Estuvo cerca —
en la verificación se editó y revirtió un `.yaml` con `git checkout --`.
**Regla nueva:** todo pedido que deba terminar en commit incluye los comandos
`git add` (con paths explícitos, nunca `git add .`) y `git commit`. Y un
`git status` pegado crudo antes de commitear.

**2. Una prueba en verde sobre una branch vacía no prueba nada.**
El plan incluía clonar la branch en limpio y correr `npm run build` para
simular Vercel. Si se hubiera ejecutado antes de descubrir lo anterior,
**habría pasado** — porque la branch clonada no tenía ni `ReadMore.astro` ni
el import que lo llama. Un sitio idéntico al del día anterior, compilando
perfecto. La prueba del clon limpio sólo vale después de confirmar que el
commit contiene lo que debe contener.

**3. El criterio del diff se enuncia sobre HTML y comportamiento, no sobre
líneas.**
El pedido decía "cualquier diferencia además de la línea del `<link>` es un
ERROR". En Fase 2b, `HeroSection.astro` importa `ReadMore` de forma
incondicional y Astro empaqueta el `<style>` scoped del componente importado
en toda página que use `HeroSection` — aunque esa rama del template no
renderice. Institucional, Proyectos y Archivo pasaron a tener 4 líneas de
diff en vez de 2: **CSS inerte, sin ningún elemento del DOM que lo matchee**
(verificado: 0 botones, 0 divs reales, mismo conteo de `<script>`).
Aceptado. Evitarlo requeriría import dinámico o partir el componente, que
cuesta más que el problema. La formulación por líneas era un proxy más
estricto que la intención.

### Estado del pedido de Caro
"Menos espaciado y botón Leer más" — el botón está completo. El menos
espaciado se cumplió **parcialmente y por efecto colateral** (las cards de
pilares bajaron de 1333px a 378px). El pedido apuntaba a la página entera:
eso es Fase 3. No darlo por saldado.

### Estado del CMS
Caro y Javier siguen sin entrar. Ahora con una condición de salida concreta:
Caro va a retocar los textos **cuando Santos le dé el OK para usar el CMS**.
Pendiente definir qué falta para levantar ese bloqueo (la guía de editores
sigue sin actualizar con las páginas nuevas y la convención de asteriscos).

## Tabla de deudas técnicas (actualizada)

| # | Deuda | Estado |
|---|---|---|
| 1 | i18n no conectado | CERRADA 03/08 |
| 2 | Páginas ES/EN duplicadas | CERRADA 03/08 |
| 3 | Tailwind 4 utilities nativas | Viva — workaround `rgb(from var())` vigente |
| 4 | Archivos `.backup` | Cerrada 27/05 |
| 5 | SVG Facebook | Cerrada 27/05 |
| 6 | Dos convenciones de énfasis (`pages` asteriscos / `projects` `<em>`) | Viva — unificar en Fase 3 |
| 7 | Pilares a 768px: columnas de ~123px | Nueva — para Fase 3 |

## Orden de trabajo actualizado
1. ~~Fase 2 — "Leer más"~~ CERRADA 04/08
2. Cerrar 1c-1: las 3 props que le faltan a `EpisodeList` (A16 `// episodios`,
   A17 `reproduciendo`, A18 `episodio`)
3. 1c-2 parcial: Institucional (7 strings) + Contacto (1 string)
4. Fase 3 — rediseño (menos espaciado, más presencia fotográfica, sacar
   badges de estado y etiquetas de pilar, layout de pilares a 768px,
   unificar convenciones de énfasis)
5. 1c-2 resto: Inicio (13 strings)
6. Actualizar la guía de editores → habilitar CMS a Caro y Javier

## Cómo retomar

```
git checkout main
git pull
npm run dev
```

Baseline en `../nautas-baseline/` regenerado desde `35df026`. Regenerarlo con
un build fresco antes de Fase 3 si hubo ediciones vía Keystatic.
